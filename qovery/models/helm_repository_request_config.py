# coding: utf-8

"""
    Qovery API

    - Qovery is the fastest way to deploy your full-stack apps on any Cloud provider. - ℹ️ The API is stable and still in development. 

    The version of the OpenAPI document: 1.0.3
    Contact: support+api+documentation@qovery.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json


from typing import Any, ClassVar, Dict, List, Optional
from pydantic import BaseModel, StrictStr
from pydantic import Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class HelmRepositoryRequestConfig(BaseModel):
    """
    HelmRepositoryRequestConfig
    """ # noqa: E501
    username: Optional[StrictStr] = Field(default=None, description="Required if the repository is private")
    password: Optional[StrictStr] = Field(default=None, description="Required if the repository is private")
    access_key_id: Optional[StrictStr] = Field(default=None, description="Required if kind is `ECR` or `PUBLIC_ECR`")
    secret_access_key: Optional[StrictStr] = Field(default=None, description="Required if kind is `ECR` or `PUBLIC_ECR`")
    region: Optional[StrictStr] = Field(default=None, description="Required if kind is `ECR` or `SCALEWAY_CR`")
    scaleway_access_key: Optional[StrictStr] = Field(default=None, description="Required if kind is `SCALEWAY_CR`")
    scaleway_secret_key: Optional[StrictStr] = Field(default=None, description="Required if kind is `SCALEWAY_CR`")
    __properties: ClassVar[List[str]] = ["username", "password", "access_key_id", "secret_access_key", "region", "scaleway_access_key", "scaleway_secret_key"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of HelmRepositoryRequestConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of HelmRepositoryRequestConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "username": obj.get("username"),
            "password": obj.get("password"),
            "access_key_id": obj.get("access_key_id"),
            "secret_access_key": obj.get("secret_access_key"),
            "region": obj.get("region"),
            "scaleway_access_key": obj.get("scaleway_access_key"),
            "scaleway_secret_key": obj.get("scaleway_secret_key")
        })
        return _obj


